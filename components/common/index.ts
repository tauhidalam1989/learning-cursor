/**
 * Common layout components for use in the global layout.
 * Re-export Navbar and Footer so the layout can import from '@/components/common'.
 */
export { Navbar } from './Navbar';
export { Footer } from './Footer';
export { navLinks, navCta } from './nav-config';
